export enum UserRoles {
  ADMIN = 'admin',
  MEMBER = 'member',
  CHARACTER = 'character',
}

export enum MovieType {
  TV = 'tv',
  MOVIE = 'movie',
}

export enum StatusType {
  COMPLETED = 'completed',
  ONGOING = 'on going',
}
