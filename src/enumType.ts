export enum UserRoles {
  ADMIN = 'admin',
  MEMBER = 'member',
}

export enum MovieType {
  TV = 'tv',
  MOVIE = 'movie',
}

export enum StatusType {
  COMPLETED = 'completed',
  ONGOING = 'on going',
}
